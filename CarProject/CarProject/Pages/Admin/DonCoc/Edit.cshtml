@page "{id:int}"
@model CarProject.Pages.Admin.DonCoc.EditModel

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8">
            <h2>Sửa Đơn Cọc</h2>
            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success">@TempData["Success"]</div>
            }
            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger">@TempData["Error"]</div>
            }
            @if (Model.DonCoc == null)
            {
                <div class="alert alert-danger">Không tìm thấy đơn cọc.</div>
            }
            else
            {
                <form method="post">
                    <input type="hidden" asp-for="DonCoc.MaDonCoc" />
                    <div class="mb-3">
                        <label class="form-label">Chi tiết xe trong đơn</label>
                        @if (Model.DonCoc.ChiTiets.Any())
                        {
                            <div style="border:1px solid #dee2e6;border-radius:6px;padding:0.75rem;">
                                @foreach (var ct in Model.DonCoc.ChiTiets)
                                {
                                    var ctTen = $"{ct.PhienBan?.DongXe?.TenDong ?? ""} {ct.PhienBan?.TenPhienBan ?? ""}".Trim();
                                    <div style="display:flex;justify-content:space-between;align-items:center;padding:0.35rem 0;border-bottom:1px solid #f0f0f0;">
                                        <div>
                                            <strong>@ctTen</strong> <small class="text-muted">x@(ct.SoLuong)</small><br>
                                            <small class="text-muted">Showroom nguồn: @(ct.ChiNhanh?.TenChiNhanh ?? ct.MaChiNhanh ?? "—")</small>
                                            @if (ct.SoTienCocPhanBo > 0)
                                            {
                                                <br><small class="text-muted">Cọc phân bổ: @ct.SoTienCocPhanBo:N0đ</small>
                                            }
                                            @if (ct.SoLuongThieu > 0)
                                            {
                                                <br><small class="text-danger">Cần nhập: @ct.SoLuongThieu xe</small>
                                            }
                                        </div>
                                        <div style="display:flex;align-items:center;gap:6px;">
                                            @if (ct.TrangThaiTiepNhan == "Đã tiếp nhận")
                                            {
                                                <span class="badge bg-success">Đã tiếp nhận</span>
                                            }
                                            else if (ct.TrangThaiTiepNhan == "Từ chối")
                                            {
                                                <span class="badge bg-danger" title="@ct.LyDoTuChoi">Từ chối <small>@ct.LyDoTuChoi</small></span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-warning text-dark">Chờ xác nhận</span>
                                            }
                                            @if (ct.DaNhapKho)
                                            {
                                                <span class="badge bg-info text-dark">Đã nhập</span>
                                            }
                                            @if (ct.SoLuongThieu > 0 && !ct.DaNhapKho)
                                            {
                                                <button type="button" class="btn btn-sm btn-outline-primary"
                                                        onclick="openNhapXe(@ct.MaChiTiet, '@(System.Text.Json.JsonEncodedText.Encode(ctTen))', @ct.SoLuongThieu)">Nhập xe</button>
                                            }
                                        </div>
                                    </div>
                                }
                            </div>
                            @if (Model.DonCoc.PhieuNhaps.Any())
                            {
                                <div class="mt-2">
                                    <strong class="small">Lịch sử phiếu nhập:</strong>
                                    @foreach (var pn in Model.DonCoc.PhieuNhaps)
                                    {
                                        <div class="small text-muted">- @pn.NgayNhap.ToString("dd/MM HH:mm") · @pn.SoLuongNhap xe - giá @pn.SoTienNhapMoiXe:N0đ (tổng @pn.TongSoTienNhap:N0đ) - @pn.NguoiNhap</div>
                                    }
                                </div>
                            }
                        }
                        else
                        {
                            <span class="text-muted">Chưa có chi tiết xe.</span>
                        }
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="DonCoc.MaKhachHang" class="form-label">Khách Hàng</label>
                            <select asp-for="DonCoc.MaKhachHang" asp-items="Model.KhachHangList" class="form-select" required>
                                <option value="">-- Chọn khách hàng --</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="DonCoc.MaPhienBan" class="form-label">Phiên Bản Xe</label>
                            <select asp-for="DonCoc.MaPhienBan" asp-items="Model.PhienBanList" class="form-select" required>
                                <option value="">-- Chọn phiên bản --</option>
                            </select>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.SoTienCoc" class="form-label">Số Tiền Cọc</label>
                            <input asp-for="DonCoc.SoTienCoc" class="form-control" type="number" step="0.01" required />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.NgayTaoDon" class="form-label">Ngày Tạo Đơn</label>
                            <input asp-for="DonCoc.NgayTaoDon" class="form-control" type="date" required />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.NgayHenNhanXe" class="form-label">Ngày Hẹn Nhận Xe</label>
                            <input asp-for="DonCoc.NgayHenNhanXe" class="form-control" type="date" />
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.PhuongThucThanhToan" class="form-label">Phương Thức Thanh Toán</label>
                            <select asp-for="DonCoc.PhuongThucThanhToan" class="form-select" required>
                                <option value="">-- Chọn --</option>
                                <option value="Chuyển khoản">Chuyển khoản</option>
                                <option value="Tiền mặt">Tiền mặt</option>
                                <option value="Cả hai">Cả hai</option>
                            </select>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.TrangThaiThanhToan" class="form-label">Trạng Thái Thanh Toán</label>
                            <select asp-for="DonCoc.TrangThaiThanhToan" class="form-select" required>
                                <option value="">-- Chọn --</option>
                                <option value="Chưa thanh toán">Chưa thanh toán</option>
                                <option value="Đã thanh toán">Đã thanh toán</option>
                                <option value="Một phần">Một phần</option>
                            </select>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="DonCoc.TrangThaiDonHang" class="form-label">Trạng Thái Đơn Hàng</label>
                            <select asp-for="DonCoc.TrangThaiDonHang" class="form-select" required>
                                <option value="">-- Chọn --</option>
                                <option value="Chờ xử lý">Chờ xử lý</option>
                                <option value="Đã xác nhận">Đã xác nhận</option>
                                <option value="Đã hủy">Đã hủy</option>
                            </select>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label asp-for="DonCoc.GhiChu" class="form-label">Ghi Chú</label>
                        <textarea asp-for="DonCoc.GhiChu" class="form-control" rows="3"></textarea>
                    </div>
                    <div class="mb-3">
                        <button type="submit" class="btn btn-primary">Cập nhật</button>
                        <a asp-page="Index" class="btn btn-secondary">Hủy</a>
                    </div>
                </form>
            }
        </div>
    </div>
</div>

<!-- Modal nhập xe -->
<div class="modal fade" id="nhapXeModal" tabindex="-1">
    <div class="modal-dialog">
        <form method="post" asp-page-handler="NhapXe" class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Nhập xe vào showroom</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" name="maChiTiet" id="nhapXeMaChiTiet" />
                <div class="mb-2" id="nhapXeTenXe"></div>
                <div class="mb-3">
                    <label class="form-label">Số lượng xe nhập</label>
                    <input type="number" class="form-control" name="soLuongNhap" id="nhapXeSoLuong" min="1" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">Số tiền nhập mỗi xe (VNĐ)</label>
                    <input type="number" class="form-control" name="soTienNhapMoiXe" id="nhapXeSoTien" min="0" step="100000" value="0" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">Ghi chú</label>
                    <input type="text" class="form-control" name="ghiChu" id="nhapXeGhiChu" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <button type="submit" class="btn btn-primary">Xác nhận nhập xe</button>
            </div>
        </form>
    </div>
</div>

<script>
    function openNhapXe(maChiTiet, tenXe, soLuongThieu) {
        document.getElementById('nhapXeMaChiTiet').value = maChiTiet;
        document.getElementById('nhapXeTenXe').innerHTML = '<strong>' + tenXe + '</strong> <span class="text-danger">(cần nhập ' + soLuongThieu + ' xe)</span>';
        document.getElementById('nhapXeSoLuong').value = soLuongThieu;
        document.getElementById('nhapXeSoTien').value = '';
        document.getElementById('nhapXeGhiChu').value = '';
        var modal = new bootstrap.Modal(document.getElementById('nhapXeModal'));
        modal.show();
    }
</script>
